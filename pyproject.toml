[project]
name = "jade"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "astropy>=7.1.1",
    "datasets>=4.4.1",
    "dm-pix>=0.4.4",
    "einops>=0.8.1",
    "matplotlib>=3.10.7",
    "orbax>=0.1.9",
    "wandb>=0.23.0",
]


[tool.setuptools]
packages = ["jade"]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
