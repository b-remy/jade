[project]
name = "jade"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "astropy>=7.1.1",
    "datasets>=4.4.1",
    "einops>=0.8.1",
    "flax>=0.12.1",
    "jax>=0.8.1",
    "wandb>=0.23.0",
]


[tool.setuptools]
packages = ["jade"]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
